# Local development frontend variables. Vercel should define VITE_API_URL in its project settings.
VITE_API_URL=http://localhost:5000/api/v1
# Allow enough time for a free-tier hosted backend to wake from sleep.
VITE_API_TIMEOUT_MS=60000
